import { HighlightdDirective } from './highlightd.directive';

describe('HighlightdDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightdDirective();
    expect(directive).toBeTruthy();
  });
});
